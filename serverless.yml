service: RewardWeeEmailServiceProvider
provider:
  name: aws
  runtime: nodejs16.x
  stage: dev
  region: us-east-1
  memorySize: 128
functions:
  app:
    handler: index.handler
plugins:
  - serverless-plugin-typescript          